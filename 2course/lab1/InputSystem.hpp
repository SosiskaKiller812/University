#include <iostream>
#include <string>

using namespace std;

bool isInt(const string &str);
void inputInt(int &value, int min = INT_MIN, int max = INT_MAX);

bool isDouble(const string &str);
void inputDouble(double &value, double min = INT_MIN, double max = INT_MAX);